# LAN Tailscale IP that Grafana binds port 3000 to. Grafana is intentionally
# NOT routed through Traefik — this is the only way to reach it.
TAILSCALE_IP=100.x.x.x

# Grafana admin login (username is fixed to "admin" in docker-compose.yml).
GF_SECURITY_ADMIN_PASSWORD=change-me
